let resultado = (5>7) ? "cierto" : "mentira";
console.log(resultado);

/*numero para con operador ternario ? */
let numero = 9;
resultado = (numero % 2 == 0) ? "número par" : "número impar";
console.log(resultado);
